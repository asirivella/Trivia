
<table class="table table-hover">
  <tr>
    <th>Email Id</th>
    <th>Score</th>
	<th>Category</th>
  </tr>
  <% @leaderboard.each do |board| %>
    <tr>
      <td><%= board.email %></td>
      <td><%= board.score %></td>
	  <td><%= board.tag %></td>
    </tr>
  <% end %>
</table>